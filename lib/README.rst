=========
Libraries
=========

These libraries implement common functionality and abstract away the kernel
interface.

The libraries are grouped by language, so C-specific libraries go into ``c/``,
Rust-specific go into ``rust/`` etc.
